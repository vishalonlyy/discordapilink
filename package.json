{
  "name": "discordwebclient",
  "version": "1.0.0",
  "description": "This is a package built with the intension of providing your discord bot to create web servers without any hardcore just use the pkg to build up",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "p":"npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
    
  },
  "author": "",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.13.0",
    "express": "^4.18.2",
    "yarn": "^1.22.19"

  },
  "devDependencies": {
    "@types/node": "^20.5.6",
    "typescript": "^5.2.2"
  }
}
